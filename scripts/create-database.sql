-- LexGuard — criação do banco de dados local
-- Execute no MySQL: mysql -u root -p < scripts/create-database.sql

CREATE DATABASE IF NOT EXISTS lexguard
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;
